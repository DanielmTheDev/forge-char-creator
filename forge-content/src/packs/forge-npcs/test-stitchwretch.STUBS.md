# Test Stitchwretch — unmatched statblock features (needs human authoring)

Inferred from the image. NO catalog match, so NOT auto-authored (security: image→statblock
never invents abilities/macros). Author each as a gate-proven forge-ability, then add its
identifier to `test-stitchwretch.json`'s `abilities` array.

## Grafted Second Head — Snapping Bite (reptilian head on the shoulder)
> A second stitched-on head makes an independent bite attack each turn (likely a bonus-action
> melee attack, piercing). No catalog ability is a bonus-action second attack.

Closest pattern = `example-strike` (melee attack), but the second-head mechanic = an EXTRA
independent attack (multiattack/bonus-action), which is a shape change knobs can't express.
Author as a new ability (or a multiattack feature — deferred to the boss-combat phase).

## Runic Staff — caster channel (the rune-carved staff it carries)
> Suggests a spell/channel action (force/necrotic bolt or a ward). Ambiguous from art alone.

Pick a base: `searing-bolt` (single-target bolt) or `radiant-rebuke` (save + damage, recharge)
if you want a magical attack — but that's an authoring choice, not an image fact. Left to the
human; add a knobbed ref once decided.
